- DBT <https://github.com/meomeo-dev/dbt-demo/blob/main/docs/dbt-%E6%95%B0%E4%BB%93%E6%95%99%E5%AD%A6/01-dbt-%E6%A6%82%E5%BF%B5%E4%B8%8E%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84.md>

- DBT 教程：<https://www.cnblogs.com/echolun/p/18019989>

- 查看进程内核栈

```
ls /proc/3312258/task/ | wc -l

# 查看每个线程的内核栈
for tid in $(ls /proc/3312258/task/); do
    echo "=== Thread $tid ==="
    cat /proc/3312258/task/$tid/stack 2>/dev/null
    echo ""
done | grep -B1 -E "shrink|reclaim|try_to_free|mem_cgroup|writeback|page_fault|oom"
```

- 内存调试 thrashing

```bash
#!/bin/bash
CGROUP="/sys/fs/cgroup/kubepods.slice/kubepods-pod87d34b03_db92_4bb3_a7f3_2830aeed75ae.slice"

echo "=== 1. 内存使用 vs 限制 ==="
echo "current: $(cat $CGROUP/memory.current)"
echo "max:     $(cat $CGROUP/memory.max)"

echo ""
echo "=== 2. 压力指标 ==="
cat $CGROUP/memory.pressure

echo ""
echo "=== 3. 事件计数 ==="
cat $CGROUP/memory.events

echo ""
echo "=== 4. 回收统计（第一次）==="
grep -E "pgscan|pgsteal|pgfault|pgmajfault|workingset_refault" $CGROUP/memory.stat

sleep 5

echo ""
echo "=== 5. 回收统计（5秒后）==="
grep -E "pgscan|pgsteal|pgfault|pgmajfault|workingset_refault" $CGROUP/memory.stat

echo ""
echo "=== 6. 进程内核栈 ==="
cat /proc/3312258/stack 2>/dev/null || echo "进程可能已退出"
```

- MemoryQoS 本质上是 K8s 对 Linux cgroup v2 内存控制接口的封装与映射

  - <https://kubernetes.io/zh-cn/blog/2023/05/05/qos-memory-resources/>

  - <https://kubernetes.io/blog/2026/04/29/kubernetes-v1-36-memory-qos-tiered-protection/>

```yaml
featureGates:
  MemoryQoS: true
```

高版本还可配置 `memoryReservationPolicy` 和 `memoryThrottlingFactor` 等策略

留 20~30% headroom

- QoS 感知调度：<https://help.aliyun.com/zh/ack/ack-managed-and-ack-dedicated/user-guide/memory-qos-for-containers>

- kubelet

```bash
kubectl get --raw "/api/v1/nodes/kube-wk-6-022/proxy/configz" | jq .
```

- 内存监控

cAdvisor 的取值（cgroup v2）：

```bash
# 和 OOM 判定同源
# 含大量可回收缓存 → 平时看着总是贴着 limit（内核本来就倾向于把内存用满做缓存）
container_memory_usage_bytes        = memory.current
container_memory_cache              = memory.stat: file
container_memory_rss                = memory.stat: anon

# 和 kubelet 驱逐一致
# 和 OOM 判定不是一个量 → 监控一片祥和，容器已被 OOMKilled
container_memory_working_set_bytes  = memory.current − inactive_file
```

关系式

```bash
usage ≈ anon(rss) + file(cache) + slab + sock + kernel_stack + percpu + ...
                     └── 注意：file 包含 tmpfs/shmem

working_set = usage − inactive_file
```

定性

```bash
# A. 总量
container_memory_usage_bytes{pod="..."}

# B. 缓存类（file + shmem）
container_memory_cache{pod="..."}

# C. 进程匿名内存
container_memory_rss{pod="..."}

# D. 驱逐 / top 视角
container_memory_working_set_bytes{pod="..."}

```

- 容器 NRI 插件。containerd
- 离线混部（全称在离线混部）是指将在线服务与离线任务部署在同一套物理机或集群资源上，通过资源隔离和动态调度技术，在保证在线业务稳定的前提下提高整体资源利用率。
  - 在线服务：如电商交易、网页浏览等。它们对响应时间（时延）极度敏感，但流量有明显的潮汐特征（白天高、夜间低），平时平均资源利用率不高。
  - 离线任务：如大数据计算、AI 模型训练、离线报表分析等。它们不直接面向用户，对时延不敏感，但会消耗大量计算资源。
  - 混合部署：让离线任务 “借用” 在线服务低峰期空闲的资源，或者通过超卖（Overcommit）压榨剩余算力

- PSI（Pressure Stall Information，压力暂缓信息）驱动的用户态 killer 是一种在系统发生严重资源紧缺前，通过内核空间上报压力指标、在用户态进行主动干预和进程终止的内存管理机制。精准评估：通过任务 “受阻塞的时长” 衡量压力，比单纯看剩余内存大小（free memory）更科学。

<https://github.com/mz1999/blog/blob/master/docs/psi.md>

压力停滞信息 (PSI) 是 Linux 内核（4.20 及更高版本）的一项功能， 它提供了一种规范化的方式来量化基础设施资源的压力， 即资源需求是否超过当前供应。 它超越了简单的资源利用率指标，而是测量任务因资源竞争而停滞的时间。 这是识别和诊断可能影响应用程序性能的资源瓶颈的强大方法。

<https://kubernetes.io/zh-cn/blog/2025/09/04/kubernetes-v1-34-introducing-psi-metrics-beta/>

```bash
grep -E 'CONFIG_PSI' /boot/config-$(uname -r) 2>/dev/null \
  || zcat /proc/config.gz 2>/dev/null | grep -E 'CONFIG_PSI'
# CONFIG_PSI=y
# CONFIG_PSI_DEFAULT_DISABLED=y

grubby --update-kernel=ALL --args="psi=1"

kubectl get --raw "/api/v1/nodes/<node-name>/proxy/metrics/cadvisor" | \
    grep 'container_pressure_cpu_waiting_seconds_total{container="cpu-stress"'
```


- torch

```bash
python -c "import torch; print(torch.version.cuda)"
```

```python
import torch    
import torch.utils     
import torch.utils.cpp_extension    
torch.utils.cpp_extension.CUDA_HOME        # 输出 Pytorch 运行时使用的 cuda
```

- victoriametrics 报错

```
422: error occured during search: cannot fetch query results from vmstorage nodes: cannot perform search on vmstorage victoria-metrics-cluster-vmstorage-1.victoria-metrics-cluster-vmstorage.cpaas-monitoring-system.svc.cluster.local.:8401: cannot execute funcName="search_v7" on vmstorage "10.4.1.163:8401": error when searching for tagFilters=[{AccountID=0,ProjectID=0,__name__="kube_job_created",namespace="hft-mbo-alpha"}] on the time range [2026-06-06T04:25:00Z..2026-06-13T05:00:00Z]: failed to search TSIDs in indexDB "2026_06": failed to search metricIDs: the number of matching timeseries exceeds 67108; either narrow down the search or increase -search.max* command-line flag values (the most likely limit is -search.maxUniqueTimeseries); see https://docs.victoriametrics.com/victoriametrics/single-server-victoriametrics/#resource-usage-limits
（跟踪 ID：46eb68fa00cf435279406e054e74c0b1）
```

- kernel clean

```
# 使用 dnf 工具自动删除旧内核，默认会保留最新的两个内核
dnf -y remove --oldinstallonly

# 重新生成并检查 GRUB 配置文件
grub2-mkconfig -o /boot/grub2/grub.cfg
```

- 拉马努金数

```
它是全世界最小、能用两种不同方式拆成两个立方数之和的自然数：1729 = 1³ + 12³= 9³ + 10³
1729 也被永久命名为“拉马努金数”，又称“的士数”(Taxicab Number)
```

- 实时行情：<https://stock-sdk.linkdiary.cn/>

- PIPX: `export PIPX_DEFAULT_PYTHON=/usr/bin/python3.13`

- bash 语法检查

```bash
bash -n
```


- gosu：实现“Root 启动，非 Root 运行”。它允许容器在初始化（Entrypoint）阶段使用 root 权限执行必要的挂载、权限修复或环境配置，随后直接退居为普通用户启动实际应用，兼顾了容器初始化灵活性与运行时的安全性

- shell

```bash
if [ -z "$*" ]; then
  echo "\
  main entrypoint
"
  exec bash --login
else
  exec "$@"
fi
# 判断 "是否没有传入任何参数"，如果没有参数则执行 then 后面的代码
```

- dnf 优化 `dnf install -y --setopt=install_weak_deps=False --setopt=tsflags=nodocs`

- dockerfile 中 `--mount=type=cache,target=/tmp,id=build-tmp`，debian 可能会因为挂载点导致装包失败，需要 `chmod 777 /tmp`

- torch

```bash
python -c "import torch; print(torch.version.cuda)"
```

```python
import torch    
import torch.utils     
import torch.utils.cpp_extension    
torch.utils.cpp_extension.CUDA_HOME        # 输出 Pytorch 运行时使用的 cuda
```

- Grafana <https://github.com/grafana/grafana-image-renderer>

```bash
docker.io/grafana/grafana-image-renderer:v5.8.8
```


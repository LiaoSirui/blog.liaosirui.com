ClickHouse 提供了 update 和 delete 的删除能力，但是和常规的例如 MySQL，Redis 这种立即见效的能力不一样。在 ClickHouse 中这种操作称为 mutation 操作

```SQL
SELECT
    database,
    table,
    command,
    create_time,
    is_done
FROM system.mutations
where is_done = 0
LIMIT 10

```


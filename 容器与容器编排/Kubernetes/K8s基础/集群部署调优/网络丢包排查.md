TCP 被动打开数

```bash
rate(node_netstat_Tcp_PassiveOpens[1m])
rate(node_netstat_Tcp_ActiveOpens[1m])
```

listen backlog 溢出

```bash
increase(node_netstat_TcpExt_ListenOverflows[5m])
increase(node_netstat_TcpExt_ListenDrops[5m])
```

SYN cookies

```bash
increase(node_netstat_TcpExt_SyncookiesSent[5m])
```

RST、重传

```bash
rate(node_netstat_Tcp_OutRsts[1m])
rate(node_netstat_Tcp_RetransSegs[1m])
```

conntrack 使用率

```bash
node_nf_conntrack_entries / node_nf_conntrack_entries_limit
```


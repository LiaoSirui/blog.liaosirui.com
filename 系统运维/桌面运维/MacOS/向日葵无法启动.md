向日葵闪退，客户端未启动

```bash
srliao at liaosirui-mbp in ~
$ sudo launchctl load -w /Library/LaunchDaemons/com.oray.sunlogin.plist
Password:

srliao at liaosirui-mbp in ~
$ sudo launchctl load -w /Library/LaunchDaemons/com.oray.sunlogin.helper.plist
```

